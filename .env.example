# Copy this file to .env in the repository root and fill in values for local development
# .env is gitignored

# Optional in local development: set an API key to protect the admin API endpoints
# Leave empty to allow all requests in development
# Deploy to Cloudflare also uses this declaration to request the production secret
API_KEY=

# Optional local CORS override:
# ALLOWED_ORIGIN=http://localhost:5173
